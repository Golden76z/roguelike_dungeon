import 'package:flame/game.dart';

/// Core Flame game. This will host the hub, dungeon, and overlays.
class RoguelikeGame extends FlameGame {
  @override
  Future<void> onLoad() async {
    await super.onLoad();
    // TODO(ch1): Load initial scene (hub) and any global resources.
  }
}

